{
  "name": "millennium-rpg-interface",
  "private": true,
  "version": "3.1.0",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check content-v3.js && node --check service-worker.js",
    "test:core": "node --test tests/core.test.mjs",
    "test:rules": "firebase emulators:exec --project demo-millennium-rpg --only firestore \"node --test tests/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.23.0"
  }
}
